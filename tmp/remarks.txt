Part A is written and verified. 55 files: README.md, CLAUDE.md, and 53 documents under knowledge/ (344 KB).

Layout
mcp/operating-rules — the must-read before any write; the one docId hard-wired into the server (resource URI, instructions, guide text, offline seed). 6971 bytes, inside the 7.5 KB resource budget.
mcp/docs/server/** (22) — install, configuration, remote mode, auth, allow levels, dry-run/confirm, audit, one doc per tool, payload conventions, response shaping, catalog, knowledge subsystem, errors, workflows, glossary, doc-map.
mcp/docs/api/** (30) — one per Admin API area, led by baseline-data: what every instance already has and must not be recreated, with the silent-duplicate vs hard-failure table (user groups, modules and attribute set types succeed silently; locales, general types, templates, menus, order statuses fail loudly), the guest group, the ~110 provisioned Content API permissions, the ungrantable export permissions, singleton settings, and the record-quota consequence.
Both subtrees sit under mcp/docs/ deliberately — the ranker reads only the first two path segments, so server and API docs both score the maximum and neither is handicapped. No back/** or front/** namespace exists, per the decision to strip the doc relations from the package.

Verification
Loaded through the published 0.1.1 binary with --knowledge-path:

cms_whoami → source: local, 53 docs / 507 sections, no warning; cms_guide reports the local clone and the "Knowledge is degraded" block is gone.
All 53 docIds read without an anchor (no has no section ""); all 507 sections read individually; none over 8 KB; none truncated (the one apparent hit is a section that documents the literal …[section truncated] marker).
Every docId#anchor cross-link resolves, including from the root files.
11 probe searches each return the intended document first. One fix came out of this: create a product originally ranked product-relations above products, because prefix matching means the query create never matches an indexed creating. Gerund headings are now imperative (## Create a product, ## Delete a page).
Redaction sweeps clean: no Cyrillic, no internal repo or infra names, no storage identifiers, no source paths, no tracker ids, no "stand". The only surviving snake_case tokens are wire-format ones (en_US, string_id42, common_page, trending_block_default, tool names).
Not done, and why
Nothing is committed or pushed. Pushing to main publishes to a public repo and changes the behaviour of every running server within one cache TTL — that's your call, not mine. git status shows M README.md, ?? CLAUDE.md, ?? knowledge/.

Part B (the dnk-mcp changes) is planned but not implemented — removing TAG_DOCS/docLinks, the string cleanups, removeComments in the build, deleting publish:knowledge, the SERVER_VERSION/metadata fixes, and deprecating npm 0.1.0. Two forward-references in the corpus already assume B1 has shipped: cms-api-describe shows the post-change next string and no docs key. Until B1 ships, cms_api_describe will still emit the 25 dead back/docs/* ids alongside mcp/operating-rules.

Say the word and I'll do Part B, or commit Part A.
